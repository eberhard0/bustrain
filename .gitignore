# --- generated data: rebuild with the pipeline scripts (see README) ---
# Raw downloads include scraped JR Kyushu timetable HTML that must not be
# redistributed; per-stop JSON under web/data is derived from it.
data/
web/data/

# user database — never publish
server/bustrain.db

# QA output & design prototypes
designs/

# local tooling
.claude/
.claude-flow/
__pycache__/
*.pyc
nohup.out
